# Week 7 — UNO Q Debugging and Computer Vision Implementation

## Goal This Week

Resolve the issues encountered with the **Arduino UNO Q** implementation and successfully integrate **computer vision**, while simultaneously testing the strength of the replaced drone frame components.

---

## What We Did

### UNO Q Debugging and Computer Vision — Agnivesh

- The **Arduino UNO Q did not respond as expected** during the initial implementation.
- **Agnivesh** investigated the issue and attempted to rectify the problem by **resetting the UNO Q** and checking the implementation.
- The existing **Raspberry Pi code was converted into Arduino UNO Q-compatible code using AI-assisted code conversion**.
- After making the necessary modifications and corrections, the UNO Q started functioning properly.
- The successful implementation allowed the team to proceed with the **computer vision system**.
- Computer vision was successfully **implemented on the UNO Q**, marking an important step in the project's software and processing development.

### Frame Strength Testing — Abhinand

- At the same time, **Abhinand** worked on testing the **strength of the replaced frame parts**.
- The replaced components were inspected and tested to determine whether they could withstand the required mechanical loads.
- The testing helped the team evaluate the suitability and reliability of the replacement parts for the drone.
- The results of the strength testing will be considered before proceeding with the final assembly and further flight testing.

---

## Initial Progress

During Week 7, the team focused on both **software implementation and mechanical validation**.

The initial response problem with the **Arduino UNO Q** was addressed by **Agnivesh** through troubleshooting and resetting the board. The Raspberry Pi code was then converted to UNO Q-compatible code using AI assistance, which resolved the implementation issue and enabled the **computer vision system** to be implemented.

In parallel, **Abhinand tested the strength of the replaced frame components** to verify their suitability for the drone structure.

---

## Problems and Blockers

- The **Arduino UNO Q did not respond properly** during the initial implementation.
- The existing Raspberry Pi code was not directly compatible with the UNO Q and required conversion and modification.
- Additional troubleshooting and resetting of the UNO Q were required before the implementation could proceed.
- The strength and reliability of the replacement frame components needed to be verified before final use.

---

## Decisions

- Continue using the **Arduino UNO Q** as part of the project implementation.
- Convert and adapt existing Raspberry Pi code to make it compatible with the UNO Q.
- Use AI-assisted code conversion as a development aid, followed by testing and correction.
- Continue with the **computer vision implementation** after resolving the UNO Q issue.
- Test and validate the strength of all replaced frame components before final assembly.

---

## Weekly Log

**Week 7:** The **Arduino UNO Q initially did not respond properly**, so **Agnivesh** worked on troubleshooting the issue and attempted to rectify it by resetting the UNO Q. The existing **Raspberry Pi code was then converted into UNO Q-compatible code using AI assistance**, after which the problem was resolved. This enabled the successful implementation of the **computer vision system**. At the same time, **Abhinand tested the strength of the replaced frame parts** to evaluate their suitability and reliability for the drone structure.
